#ifndef __COMMON__
#define __COMMON__

#include <unordered_set>
#include <set>
#include <vector>
#include <string>
#include <ostream>
#include "external/prettyprinter/containers.h"

namespace ltsy {
    using Symbol = std::string;
    using Arity = int;
};

#endif
